<ContentDialog
    x:Class="MiAir.WinUI.Views.QrLoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="扫码登录小米账号"
    CloseButtonText="取消"
    DefaultButton="None"
    CornerRadius="12">

    <StackPanel Spacing="16" Width="300" HorizontalAlignment="Center" Margin="0,12,0,8">
        <Border CornerRadius="8"
                BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="White"
                Padding="12"
                HorizontalAlignment="Center"
                Width="240" Height="240">
            <Grid>
                <!-- QR Code Image -->
                <Image x:Name="QrImage" Stretch="Uniform" Visibility="Collapsed"/>
                
                <!-- Loading Ring -->
                <ProgressRing x:Name="QrProgressRing" IsActive="True" Width="40" Height="40"/>
            </Grid>
        </Border>

        <!-- Status Hint -->
        <TextBlock x:Name="StatusHintText"
                   Text="正在生成登录二维码..."
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"/>

        <TextBlock Text="请使用【米家 App】或【微信】扫描上方二维码授权登录"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   TextWrapping="Wrap"/>
    </StackPanel>
</ContentDialog>
